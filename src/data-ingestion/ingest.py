import os
# Chúng ta sẽ dùng LangChain để xử lý PDF
# pip install langchain PyPDF2 qdrant-client sentence-transformers

def process_gout_data():
    # 1. Đọc file PDF (Giả sử bạn upload file vào thư mục data/)
    # 2. Cắt nhỏ văn bản (Text Splitting)
    # 3. Chuyển thành Vector (Embedding)
    # 4. Đẩy vào Qdrant
    print("Đang xử lý dữ liệu y khoa từ QĐ 361...")
    pass

if __name__ == "__main__":
    process_gout_data()
